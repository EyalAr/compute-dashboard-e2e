describe('Login', () => {
  describe('failure', () => {
    it('should display the login form');
    it('should keep displaying the login form, with an error message, if wrong credentials');
  });
});
